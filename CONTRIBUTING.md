# Contributing

1. Run `npm install`.
2. Run `npm run setup:local`.
3. Run `npm run lint`, `npm run typecheck`, and `npm test` before sending changes.
4. Keep provider-specific SDK imports inside provider packages or deployment adapters.
5. Add tests for provider behavior, artifact compilation, and migration helpers when changing those areas.
