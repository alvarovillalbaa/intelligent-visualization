# Convex Provider

This package is the only allowed location, alongside `convex/**`, for Convex SDK imports.

Status: migration boundary created; executable adapter work remains. The current application still has legacy Convex access in `lib/repository.ts` until the full service refactor is completed.
