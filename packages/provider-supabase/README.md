# Supabase Provider

This package is the only allowed location for `@supabase/supabase-js` and PostgreSQL provider-specific imports.

Status: provider boundary created; executable adapter, migrations, storage adapter, local Supabase scripts, and contract tests remain.
