export const cloudflareProviderStatus = {
  kind: "cloudflare",
  implemented: false,
  note: "Cloudflare Worker + D1 support is planned behind the provider contract and is not advertised as passing yet.",
} as const
