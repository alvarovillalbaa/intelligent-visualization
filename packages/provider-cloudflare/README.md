# Cloudflare Provider

This package owns the Next.js-side client for the Cloudflare Worker + D1 persistence gateway.

Status: boundary created; signed Worker RPC, D1 migrations, R2 adapter, and contract tests remain.
