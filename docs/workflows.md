# Workflows

Planned workflows:

- `sourceIngestionWorkflow`
- `visualizationGenerationWorkflow`
- `visualizationRewriteWorkflow`
- `visualizationPublishWorkflow`

Current status: not implemented.

Local mode will use a deterministic workflow adapter when hosted Vercel Workflows are unavailable.
