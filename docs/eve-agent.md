# Eve Agent

Planned durable agent: `Visualization Director`.

Current status: not implemented.

The agent will live under `agent/` and use typed tools over application services. It must not import persistence provider SDKs.
